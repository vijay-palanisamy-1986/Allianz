---
version: 0

models:
  - name: sat_product_discount_v0
    description: Product Discount satellite table
    config:
      contract:
        enforced: true
    meta:
      technical_owner: PIM Team

    columns:
      - name: product_discount_hk
        description: Hask difference of the record
        data_type: binary
        tests:
          - not_null
          - unique

      - name: product_hk
        description: Hask key of the Product Discount satellite table
        data_type: binary
        tests:
          - not_null

      - name: DV_LOAD_DATETIME
        description: Time stamp, when the record is loaded into DWH Stage layer
        data_type: timestamp_ntz

      - name: DV_RECORD_SOURCE
        description: Source system of the record
        data_type: string

      - name: discount_name
        description: Name of the discount
        data_type: string
        tests:
          - not_null

      - name: discount_percentage
        description: discount percentage value
        data_type: number(38,2)
